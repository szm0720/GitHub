server:
  port: 9003

spring:
  cloud:
    config:
      discovery:
        enabled: false
        service-id: config
  application:
    name: springboot_rabbitmq
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: firmiana
    template:
      exchange: firmiana

